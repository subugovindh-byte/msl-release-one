{
  "name": "@modernex/shared",
  "version": "2.0.0",
  "type": "module",
  "description": "Shared types, validation schemas, and GST logic",
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "build": "echo 'Shared is JS-only — no build step'",
    "test": "echo 'Shared tests run in consumer packages'"
  },
  "dependencies": {
    "zod": "^3.22.4"
  }
}
